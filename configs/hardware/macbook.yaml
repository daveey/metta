# @package __global__

framework:
  sample_factory:
    device: cpu
    decorrelate_experience_max_seconds: 0
    num_workers: 3
    save_every_sec: 30

  pufferlib:
    device: cpu
    train:
      num_envs: 1
      num_workers: 1
      env_batch_size: 1
      update_epochs: 1
      batch_size: 1024
      minibatch_size: 1024
      checkpoint_interval: 10
